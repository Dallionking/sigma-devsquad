
import React from 'react';
import { FirstAgentFormContainer } from './FirstAgentFormContainer';
import { FirstAgentFormProps } from './types';

export const FirstAgentForm = (props: FirstAgentFormProps) => {
  return <FirstAgentFormContainer {...props} />;
};
